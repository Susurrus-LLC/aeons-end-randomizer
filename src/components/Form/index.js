import React from 'react'

import Options from '../Options'

import './form.sass'

const Form = () => (
  <form
    id='form'
    name='form'
    className='form'
  >
    <Options />
  </form>
)

export default Form
